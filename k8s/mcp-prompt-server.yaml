apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-prompt-server
  namespace: sre-agent
  labels:
    app: mcp-prompt-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-prompt-server
  template:
    metadata:
      labels:
        app: mcp-prompt-server
    spec:
      containers:
        - name: mcp-prompt-server
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/mcp/prompt-server
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: prompt-server
  namespace: sre-agent
spec:
  selector:
    app: mcp-prompt-server
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
